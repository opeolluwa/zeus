use crate::config::database::mongodb;
use crate::shell::AuthLogin;
use mongodb::bson::doc;
use dialoguer;
///accept username and password from cli and log in a user
 pub async fn login(credentials: AuthLogin) {
    //see if user already exists

}

pub fn sign_up() {}
