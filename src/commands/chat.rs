use crate::shell::{BeginChat, ChatCommands, JoinChat, SendChatInvitation};

fn chat() {
    todo!();
}

pub fn handle_chat(sub_command: ChatCommands) {
    //destructure the command type from the sub_command argument
}
